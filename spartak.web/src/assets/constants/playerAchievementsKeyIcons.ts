import { IconFlag } from "../icon/iconFlag";
import { IconMedal } from "../icon/iconMedal";
import { IconTrophy } from "../icon/iconTrophy";

export const playerAchievementsKeyIcons: Record<string, (value: any) => any> = {
  trophy: IconTrophy,
  flag: IconFlag,
  medal: IconMedal,
};
